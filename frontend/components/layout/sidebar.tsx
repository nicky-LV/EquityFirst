import {GridItem} from "@chakra-ui/react";

const Sidebar = (props) => {
    return (
        <GridItem
            rowSpan={12}
            colSpan={1}
            style={{display: "flex", flexDirection: "column"}}
        >

        </GridItem>
    )
}

export default Sidebar